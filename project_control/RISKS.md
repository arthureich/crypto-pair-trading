# Risks

## Active Risk Register

| Risk | Severity | Owner | Mitigation | Status |
|---|---|---|---|---|
| Signal sends or implies direct order action | Critical | Architect Agent | Explicit SignalIntent-only interface | Open |
| Order sent without persisted event | Critical | Ledger Agent | Event-first execution contract | Open |
| ACK_UNKNOWN retried blindly | Critical | Execution / Risk Agent | Force reconciliation before retry | Open |
| Fill applied as blind delta | Critical | Ledger Agent | Cumulative executedQty reconciliation | Open |
| Stale or gapped book permits entry | High | Market Data Agent | Book health gate | Open |
| Model used for hard stop or hedge | Critical | Execution / Risk Agent | Deterministic exit invariant | Open |
| Recovery boots into trading before reconciliation | Critical | Ledger Agent | Safe mode boot protocol | Open |
| REST 5xx treated as no fill | Critical | Ledger Agent | Reconcile by order id and cumulative fills before assuming state | Open |
| Missing WebSocket event treated as no fill | Critical | Ledger Agent | REST reconciliation and cumulative executedQty checks | Open |
| Orphan order cancel treated as flat without requery | Critical | Ledger Agent | Safe orphan cancel requires cancel by exchange order id, then requery order/fill state and positions | Open |
| Safe mode permits non-risk-reducing action | Critical | Ledger Agent | Recovery protocol allows only cancellation, reconciliation, and proven risk reduction | Open |
| Recovery resumes without FLAT_RECONCILED or equivalent reconciled state | Critical | Ledger Agent | Normal resume requires zero ACK_UNKNOWN, applied cumulative fills, position match, orphan resolution, and persisted reconciled state | Open |
| Partial fill does not trigger hedge or lockdown | Critical | Execution / Risk Agent | Explicit PARTIALLY_FILLED and HEDGING_REQUIRED routes | Open |
| Ledger uncertainty permits new entry | Critical | Execution / Risk Agent | Fail-closed entry gate on ledger uncertainty | Open |
| Kill switch unavailable or fail-open | Critical | Execution / Risk Agent | Deterministic local kill switch and external heartbeat requirement | Open |
| Missing or stale risk input permits entry | Critical | Execution / Risk Agent | `docs/risk_limits.md` requires missing/stale risk inputs to fail closed | Open |
| Risk-reducing action increases exposure | Critical | Execution / Risk Agent | Stress-risk proof obligation: new stress risk must be lower than old stress risk | Open |
| Forbidden runtime configuration accepted | Critical | Execution / Risk Agent | Reject Cross Margin, Kelly, 10x, live multi-exchange, and leverage before Sprint 26 at config load and pre-order checks | Open |
| Daily loss or drawdown threshold unresolved before live mode | High | PM Agent | Mark as Sprint 1 gate unresolved; live entries fail closed until measurable thresholds are approved | Open |
| Scope drift across agents | Medium | PM Agent | Allowed-file lists and handoffs | Open |
| Binance Public Data bookTicker source has no verified top-of-book/L2 coverage past ~2024-04, so cost-gated PASS cannot be produced for the full 2023-06/2026-05 window from this source | High | PM Agent | Per ADR-0007, cost-gated claims are scoped to what was actually verified (see next row); 2024-05 through 2026-05 needs an alternative source or live-capture evidence | Open |
| Genuine cost-gated PASS for 31 candidate pairs rests on a single verified month (June 2023), not a multi-year sample; spread regimes can differ across market conditions not covered by this pilot | Medium | Quant Research Agent | Documented explicitly in reports/research_sprint_07.md and BLOCKERS.md as a 1-month pilot; do not generalize to full-window or all-regime claims without processing more verified months | Open |
| Stitching daily bookTicker archives produces a small number of duplicate/residual hourly rows at day boundaries (27 duplicate symbol-hours / 54 duplicate rows out of 10827 raw hourly rows in the expanded June 2023 pilot) | Low | Market Data Agent | Expanded gate uses an explicit deduplicated hourly-cost file (10800 rows), keeping the row with the highest spread_sample_count_1h and latest last_event_time per (symbol, open_time); raw duplicates are preserved in `all_candidates_202306_duplicate_hours.csv` for audit | Open |
| S3 ListObjectsV2 pagination (IsTruncated/NextContinuationToken) is not handled in `_fetch_s3_objects`/`parse_s3_list_objects` | Medium | Market Data Agent | Verified harmless for the current run (KeyCount << MaxKeys=1000, IsTruncated=false); add continuation-token handling before relying on this probe if per-symbol object counts could exceed 1000 | Open |
| `download_archives`/`_download_archive` real-network path in historical_dataset.py has zero test coverage (no mock) | Medium | QA Agent | Safe for offline CI since no test exercises it; add a mocked-network regression test before depending on this path for unattended/scheduled real downloads | Open |
| Sprint 8 backtest holds a fixed 1-hour horizon with no stop-loss/take-profit/z-score-reversion exit | Medium | Backtest Agent | Documented as a known first-pass simplification in reports/sprint_08_backtest.md; a proper BarrierPolicy-driven exit (already specified in docs/architecture.md for live use) is not yet wired into the offline backtest | Open |
| Sprint 8 execution cost uses only median hourly top-of-book spread, not p95/p99 tail or order-book depth/impact for the traded notional | Medium | Market Data Agent | Documented explicitly; a p95-based sensitivity re-run is a reasonable follow-up before allocating capital to the 13 backtest-approved pairs | Open |
| Sprint 8 max_drawdown_bps is computed per pair only; no time-aligned combined portfolio drawdown metric exists across the 622 simulated trades | Low | Backtest Agent | Documented in reports/sprint_08_backtest.md; do not read per-pair drawdown as a portfolio risk figure | Open |
| 17GB of raw checksum-verified Binance bookTicker archives are preserved under data/research/binance_public/cost_pilot/raw/ pending TASK-008-08 cleanup | Low | Market Data Agent | Cleanup intentionally BLOCKED until the user explicitly accepts the evidence state; disk has 353GB free, not urgent | Open |
| Roadmap canonical Sprint 8 gap: directional triple-barrier exit and Sharpe/Sortino/profit-factor metrics were never implemented (this project's actual "Sprint 8" was a different, hybrid design) | Medium | Backtest Agent | Per ADR-0009, built retroactively as separate TASK-008C-* work (`src/research/triple_barrier.py`, `src/backtest/statistical_backtest.py`); real run against all 41 Sprint 7 statistical pairs completed, `reports/backtest_statistical.md` written, gate is NAO PASSA (0/41 pairs) | Closed |
| Canonical Sprint 8's statistical_backtest metrics (trade_count/turnover/hit_rate/profit_factor/Sharpe/Sortino) assume unconstrained concurrent exposure -- every threshold-crossing bar opens its own independent trade with no open-position tracking, so trades overlap in time | Medium | Backtest Agent | Documented explicitly in the module docstring and reports/backtest_statistical.md; do not read these numbers as what one deployed strategy instance would realize | Open |
| Canonical Sprint 8's conservative fixed fee/slippage constant (6.0bps/leg roundtrip) is an unmeasured assumption, and is plausibly on the cheap side versus real Binance USD-M taker fees (~4-5bps/side before slippage) | Low | Backtest Agent | Documented in reports/backtest_statistical.md Section 3.3; since the gate already fails all 41 pairs even at this lenient assumption, real costs would only reinforce the negative conclusion, not overturn it | Open |
| Signal diagnostics use ex-post fields (`bars_held`, `outcome`, PnL) that explain realized trades but cannot be used as entry features without look-ahead | High | Quant Research Agent | Addressed by TASK-SIG-002/003/004: all follow-ups used causal config changes or bounded replication checks, never ex-post trade filtering; Signal Iteration 1 is closed as rejected (ADR-0010) | Closed |
| Sprint 9 executable backtest uses aggressive MARKET_IOC on both legs of both entry and exit -- the most expensive execution style tested | High | Execution / Risk Agent | Per ADR-0011, Sprint 10 Block 1 tested a passive/maker `LIMIT_MAKER_TTL` variant against the same 13 pairs/signals/real data. Result (`reports/passive_execution_variant.md`): still 0/13 pairs net-positive; portfolio net PnL improves +$260.35 (~11.5%) but total unclosed residual quantity increases 27%. Closes the open question -- the 0/13 result is not solely an artifact of maximally aggressive execution | Closed |
| `LIMIT_MAKER_TTL` passive execution increases aggregate unclosed residual ("naked leg") quantity by 27% versus `MARKET_IOC` (11,470.92 -> 14,565.31 units) -- driven mainly by `ARBUSDT/LINKUSDT` (+2,497) and `ARBUSDT/DOTUSDT` (+1,580); 8 of 13 pairs actually reduce residual under the passive style, so the effect is pair-dependent, not uniform | High | Execution / Risk Agent | Documented in `reports/passive_execution_variant.md`; a future Hedge Engine/Barrier Manager/Emergency Exit (Sprints 21-22) must account for execution-style-dependent residual risk, not assume passive execution is strictly safer | Open |
| `LIMIT_MAKER_TTL`'s `limit_ttl_ms=60,000` (60s) is a documented assumption, not calibrated against real production order-placement/cancel-replace telemetry -- 65 entry legs and 36 exit legs expired unfilled within this window in the real run | Medium | Execution / Risk Agent | Must be recalibrated against real order-placement data (same caveat already logged for Sprint 9's latency/ACK_UNKNOWN assumptions) before use in the Sprint 10 Execution Risk Gate; no TTL parameter sweep was run | Open |
| Sprint 9 unclosed residual position quantity (11,470.92 units across 239 trades) is not marked to market in the reported net PnL -- a real, uncovered "naked leg" exposure equivalent to what a live Hedge Engine/Barrier Manager/Emergency Exit would need to resolve | High | Execution / Risk Agent | Documented as a first-class risk in reports/backtest_executable_v1.md, not a footnote; do not promote this universe to paper/live trading without a mechanism to force-close or hedge residual legs | Open |
| Sprint 9 latency (250ms fixed) and ACK_UNKNOWN rate (2%) are modeled assumptions with no real production telemetry to calibrate them | Medium | Execution / Risk Agent | Must be recalibrated against real order-placement data before use in the Sprint 10 Execution Risk Gate; do not treat as validated inputs | Open |
| `simulate_limit_fill` hardcodes `slippage_bps=0.0` regardless of reference price, an inconsistent metric definition versus MARKET_IOC orders | Low | QA Agent | Fixed in Sprint 10 (ADR-0011): `simulate_limit_fill` now accepts an optional `reference_price` and computes `slippage_bps` with the same sign convention as `MARKET_IOC`; defaults to `None` (not a fabricated `0.0`) when no reference is given | Closed |
| Funding-rate carry (TASK-FUND-002): gross edge (funding + a correlated price component) is real and positive at every tested K, but is fully erased by the pre-registered cost of a 100% full rebalance every 8h for 3 years (19,722.00 bps cost vs. 8,992.18 bps gross at K=5) | Medium | Backtest Agent | Per TASK-FUND-003, incremental (yield-threshold) rebalancing cut cost 99.83% (19,722.00 -> 33.60 bps) and flipped net PnL positive (+5,620.99 bps), but net profit factor (1.0904) still falls 0.0096 short of the 1.10 gate at the pre-registered K=5 -- see `reports/funding_carry_incremental_backtest.md` | Closed |
| Funding-rate carry incremental (TASK-FUND-003): K=5 (primary, decisive) net profit factor 1.0904 is a precise near-miss of the 1.10 gate (not an underpowered-sample artifact -- 3,287 resolved rebalances, 6.57x the pre-registered 500 floor); K=3 (descriptive-only) clears the gate at 1.1356 | Medium | Quant Research Agent | Per the pre-registered rule (ADR-0010 discipline), K=3 passing is not substituted for the K=5 gate decision. A follow-up testing an intermediate K (e.g. K=4) or a different cost/threshold assumption would require its own new, separately pre-registered task -- not an automatic next step | Open |
| Funding-rate carry incremental's "one swap = one full round-trip cost" convention (`_apply_yield_threshold_swaps`/bootstrap in `src/research/funding_carry.py`) is an explicit modeling choice, not a measurement -- a real swap might cost less than a fresh full round-trip | Low | Backtest Agent | Documented in `reports/funding_carry_incremental_backtest.md` Risks; likely conservative (biased toward higher cost, consistent with this project's convention elsewhere), but not verified against real tick-level execution | Open |
| Funding-rate carry's observed price-return correlation (symbols ranked highest/lowest by funding tend to move favorably for the carry book over the next 8h) has no separate significance or out-of-sample test -- it is a descriptive observation from one sample window (2023-06/2026-05), not a validated standalone signal | Medium | Quant Research Agent | Documented explicitly in `reports/funding_carry_backtest.md` Risks; do not treat as confirmed alpha without a dedicated follow-up test | Open |
| TSMOM Donchian breakout (24h channel, 3x ATR trailing stop): win rate 34.30% clears the pre-registered 30% floor (confirming the intended cut-losses/let-winners-run asymmetry manifested) but net profit factor 1.005 is far below the 1.20 gate, and max drawdown (85,654.62bps) is ~10.5x the total net profit (8,187.26bps) | Medium | Backtest Agent | Documented in `reports/tsmom_backtest_final.md`; gate NAO PASSA decisively (not a near-miss); per-symbol heterogeneity (9/20 negative, 11/20 positive) is expected variance, not a basis for a post-hoc symbol filter | Closed |
| TSREV Research Family C (ADR-0014): primary cell (Family A, time-series reversal, 24h, out-of-sample) shows a real, temporally-stable reversal signature (win rate 52.68% OOS vs 52.71% in-sample, nearly identical) but net profit factor 1.0143 fails the 1.05 gate and max drawdown (65,719.66bps) is ~6x an equal-weight buy-and-hold benchmark (11,003.94bps) over the same OOS period | Medium | Quant Research Agent | Documented in `reports/tsrev_backtest_final.md`; the in-sample/OOS net PnL sign flip (-48,496.48 vs +7,690.14bps) with stable win rate validates the OOS-split methodology's value. Gate NAO PASSA is final per the pre-registered rule; no re-test with a different horizon on this exact hypothesis | Closed |
| TSREV secondary cell (Family A, 48h, descriptive-only) clears 2 of 4 gate criteria (net PF 1.09, net PnL +36,341.74bps) but still fails the same drawdown-vs-baseline criterion (71,610.74bps vs 11,003.94bps) as the primary cell; the same "longer horizon performs better but still fails drawdown" pattern also appears in Family B (cross-sectional) | Medium | Quant Research Agent | Per the pre-registered rule (ADR-0014), this cannot be promoted or substituted for the primary result. A legitimate candidate for a new, separately pre-registered task testing longer horizons with an explicit risk-control mechanism -- not an automatic next step | Open |
| `LIMIT_MAKER_TTL`'s "never crosses at placement" invariant has a latent gap: if `latency_ms=0` AND the pricing quote itself has zero spread (`best_bid == best_ask`, permitted by the `TopOfBookQuote` schema), the pricing quote's own event_time can satisfy the crossing-candidate window, letting the order fill against the exact instant it was priced from | Low | Independent adversarial review (this session) | Not exploitable under the production config used for the real Sprint 10 report (`latency_ms=250` default, never 0); not exercised by any existing test. Add a regression test and either reject zero-spread quotes for passive pricing or exclude the pricing quote's own event_time from its own crossing window before relying on `latency_ms=0` configs | Open |
| Research Family D / Payoff Engineering Fase 1 (TASK-PAYOFF-001): the strongest actionable cuts found (SHORT-only, exclude BTC/ETH, exclude highest-liquidity quartile) were identified by observing performance INSIDE the TSREV primary cell's own out-of-sample period (2025-06/2026-05) | High | PM Agent | `TASK-PAYOFF-002`/ADR-0016 pre-registers a genuinely new OOS validation (SHORT-only primary, 3 descriptive secondaries) per this exact caveat -- execution is explicitly BLOCKED until real data exists past 2026-05-31 with >=500 new resolved trades; the user chose to wait rather than download a thin sample now or use an internal holdout | Closed |
| Research Family D / Payoff Engineering Fase 2 (TASK-PAYOFF-002) cannot run yet: the normalized dataset ends 2026-05-31 and no real data exists past that date for the full 20-symbol universe as of 2026-07-05 -- any "new OOS" claim before the dataset is extended would silently reuse the same contaminated window | Medium | PM Agent | Design fully locked now in `docs/pre_registers/TASK-PAYOFF-002.md` (primary cell, secondaries, gate, resume trigger of >=500 new trades) so the decision is never made after seeing results; task status is BLOCKED in `TASK_BOARD.md`, not READY/IN_PROGRESS -- no code, no download, no execution until the trigger is met | Open |
| Research Family E / TASK-CS-001 (Cross-Sectional Momentum replication): only 20 symbols vs. hundreds typically used in the published literature, and the tested period (2023-2026, USD-M perpetuals) is more recent/more arbitraged than the original paper's 2014-2018 sample -- a weaker/absent replicated effect may reflect these differences, not proof the effect never existed in crypto broadly | Medium | Quant Research Agent | Documented explicitly in `reports/cs_momentum_backtest_final.md`; the negative result is scoped to THIS universe/period/breadth, consistent with the same "more liquid/more arbitraged" reading already used for BTC/ETH underperformance in the Payoff Engineering Phase 1 report | Open |
| Research Family E / TASK-CS-001: gate NAO PASSA decisively -- net profit factor 0.98 (below breakeven), and unlike every other family tested this session, the GROSS (pre-cost) OOS return is also negative (-64.61bps) -- no directional edge exists to be eaten by cost, unlike TSMOM/Funding Carry where a real gross edge was consumed by turnover cost | Low | Backtest Agent | Documented in `reports/cs_momentum_backtest_final.md`; win rate ~50% in both in-sample (49.40%) and out-of-sample (51.96%) periods reinforces the noise-level reading; per the pre-registered rule, no parameter sweep (horizon, K, weighting) was run after seeing this result -- CS-002 through CS-005 remain planned backlog, not started | Closed |
| Research Family E / TASK-CS-002: a same-horizon mirror of CS-001 (168h, sides swapped) would have had its OOS net PnL determined by mathematical construction (`gross_reversal = -gross_momentum`, identical cost) BEFORE running any backtest -- an uninformative test if used unchanged | Low | PM Agent | Caught before writing the pre-registration; verified numerically (`net_reversal_mirror = -241.39bps`) and disclosed in `docs/pre_registers/TASK-CS-002.md`/ADR-0018; the user chose a genuinely distinct 24h horizon instead, consistent with the literature's own separation of short-term reversal and medium-term momentum as different time-scale phenomena, not the same signal sign-flipped | Closed |
| Research Family E / TASK-CS-002: this project already had a descriptive (never gated) result at the same 24h horizon before CS-002 ran -- TSREV Family B (`TASK-TSREV-002`, ADR-0014), z-score ranking, decile k=2, full-sample: profit factor 0.87, net PnL -9,035.01bps, directionally negative | Low | Quant Research Agent | Not the same test (raw return vs. vol-normalized ranking, k=4 vs k=2, OOS-only vs full-sample), but close enough to disclose explicitly in the pre-registration BEFORE CS-002's own backtest ran, so the eventual result (also NAO PASSA, PF 0.94 OOS) is read as independently corroborating, not as a surprise or as evidence CS-002 was chosen with hindsight | Closed |
| Research Family E / TASK-CS-002: gate NAO PASSA across all three cuts (OOS PF 0.94, in-sample PF 1.00, full-sample PF 0.98) -- never crosses the 1.10 floor in any period; gross (pre-cost) OOS PnL also negative (-801.35bps), same pattern as CS-001 | Low | Backtest Agent | Documented in `reports/cs_reversion_backtest_final.md`; with both CS-001 and CS-002 closing NAO PASSA and neither showing a detectable gross edge, the classic-price-factor research line (cumulatively Families A, Funding Carry, TSMOM, C/TSREV, E) closes this session per the user's proposed roadmap and ADR-0018; CS-003/004/005 remain formally uncancelled backlog but the recommended next step (not automatic) is a genuinely new information category (Market Microstructure/Alternative Data) | Closed |
| The user proposed broadening the trading universe toward smaller-cap/lower-liquidity symbols, where crypto factor literature often documents larger gross effects, but explicitly declined to do so now (larger spreads/slippage/operational risk, and it would break comparability with every result already produced in this project) | Low | PM Agent | Recorded as a legitimate future direction in `docs/pre_registers/TASK-CS-002.md` and `project_control/DECISIONS.md` ADR-0018; not started, no universe change has been made to any existing dataset or backtest | Open |
| Research Phase II / Family I (Liquidation Dynamics): the `liquidationSnapshot` prefix (daily and monthly, checked with no symbol filter) is completely empty in Binance's public data bucket for every symbol -- no historical bulk liquidation data is available from this project's established data pipeline | Medium | PM Agent | Confirmed via a real read-only S3 listing probe before any pre-registration was written (no download committed); documented in `project_control/DECISIONS.md` ADR-0019; Family I is formally BLOCKED (not cancelled) -- pursuing it would require either a forward-only WebSocket `forceOrder` capture (no history, months to accumulate) or an unauthorized third-party vendor, neither started | Open |
| Research Phase II / TASK-ALT-001 (Family G, Funding Structure): `funding_price_divergence` shows a positive, remarkably stable Spearman correlation across 3 independent non-overlapping ~12-month sub-periods (0.0276 / 0.0230 / 0.0239) and the full sample (0.0248), but falls 0.0052 short of the pre-registered 0.03 "has information" threshold | Low | Quant Research Agent | Documented in `reports/alt_info_funding_structure_diagnostic.md`; classified strictly as SEM_INFORMACAO per the pre-registered rule, no threshold adjustment after seeing the number (same discipline as the Funding Carry incremental near-miss, TASK-FUND-003); flagged as a well-motivated candidate for a SEPARATE future task, not a retest of this one | Closed |
| Research Phase II / TASK-ALT-001: 3 of 4 candidate funding features (`funding_extreme`, `funding_reversal`, `funding_acceleration`) show a SIGN FLIP across the 3 sub-periods -- the full-sample correlation for any of these alone would have been misleading without the sub-period stability check | Low | Backtest Agent | Documented in `reports/alt_info_funding_structure_diagnostic.md`; this is exactly the spurious-full-sample-effect failure mode the pre-registered sub-period sign-consistency criterion was designed to catch, validating the methodology choice (ADR-0019) | Closed |
| Research Phase II / TASK-ALT-002 (Open Interest): the `metrics` public-data family is published as DAILY archives only (monthly listing is empty for every symbol checked) -- a new, dedicated downloader was required rather than reusing `historical_dataset.py`'s monthly-oriented `BinanceArchiveSpec` unchanged | Low | Backtest Agent | Confirmed via a real S3 listing probe before writing the pre-registration; `scripts/download_alt_open_interest.py` is a separate, purpose-built downloader that reuses only the generic, family-agnostic `verify_checksum_file` -- no changes made to the existing monthly-archive machinery | Closed |
| Research Phase II / TASK-ALT-002: a datetime-to-epoch-ms conversion bug was caught during a pre-download smoke test (1 symbol, 3 days) -- `.view("int64")` fails on tz-aware datetime64 Series in pandas 3.0, and even `.astype("int64")` without forcing a unit would have silently produced microseconds instead of milliseconds (pandas 3.0 infers a coarser default unit for sub-second-free timestamp strings) | Low | PM Agent | Caught before the real ~22,000-file download ran, not after; fixed by forcing `.astype("datetime64[ms, UTC])` before the int64 cast, verified against the known epoch-ms convention already used throughout this session's fixtures (`1685577600000` = 2023-06-01T00:00:00Z) | Closed |
| Research Phase II / TASK-ALT-002: none of the 5 Open Interest features meet the pre-registered information-content threshold; the strongest (`oi_delta`) is 0.0111 short of 0.03 -- more than twice as far as Family G's near-miss -- and `oi_delta`/`oi_acceleration` show a monotonically DECAYING correlation magnitude across the 3 sub-periods (e.g. oi_delta: -0.0321 -> -0.0202 -> -0.0048), nearly zero in the most recent period | Low | Quant Research Agent | Documented in `reports/alt_info_open_interest_diagnostic.md`; read as consistent with the same growing-market-efficiency narrative already used for BTC/ETH underperformance in the Payoff Engineering Phase 1 report, not acted on as a basis to reopen this task with adjusted parameters | Closed |
| Research Phase II download/parsing scripts (`download_alt_open_interest.py`) have no test coverage on the real-network path (no mock) | Low | QA Agent | Same accepted precedent already documented for `historical_dataset.py`'s real-network path elsewhere in this file; pure-computation logic (date ranges, URL construction, hourly resampling, epoch conversion) is fully unit-tested in `tests/test_download_alt_open_interest.py` (13 tests) | Open |
| Payoff attribution's loss-concentration (Pareto) helper originally computed `(cumulative <= threshold).sum() / n`, counting all positions in the monotonic cumulative-loss series satisfying the condition rather than only the first crossing, producing the logically impossible `frac_50 (81.1%) > frac_80 (54.5%)` | Low | Backtest Agent | Found and fixed during TASK-PAYOFF-001 by adding `_first_crossing_fraction()` (uses `argmax()` on a boolean array to find the first crossing index); re-run confirmed monotonic, valid result (`frac_50=19.0% < frac_80=45.5%`) | Closed |

## MVP Forbidden Items

```text
Cross Margin
Kelly sizing
10x leverage
leverage before Sprint 26
blind order retry
live multi-exchange
model-driven emergency exit
```
